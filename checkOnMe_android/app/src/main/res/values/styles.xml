<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Full screen theme for the splash, login and sign_up activities -->
    <style name="AppTheme.FULL_SCREEN">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- A style for the Navigation Menu Items -->
    <style name="NavMenuItems">
        <item name="android:layout_width"> match_parent </item>
        <item name="android:layout_height"> 48dp </item>
        <item name="android:textSize"> 20sp </item>
        <item name="android:gravity"> center_vertical </item>
        <item name="android:textColor"> @color/drawer_menu_item_color </item>
        <item name="android:background">@drawable/drawer_menu_item_background</item>
        <item name="android:padding"> 10dp </item>
    </style>

</resources>
